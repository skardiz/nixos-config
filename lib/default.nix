# lib/default.nix
# Наша будущая библиотека кастомных вспомогательных функций.
# Аргументы, которые мы передаем, чтобы наши функции имели к ним доступ.
{ pkgs, lib, ... }:

{
  # Здесь пока пусто, но мы готовы к будущему!
  # myHelpers.generateUser = user: { ... };
}
